# 프로젝트 명세

Threat Feed Relay(TFR)는 외부 위협 지표를 수집·정규화·보강하고,
출처와 변경 이력을 유지하면서 Profile별 Output Feed로 재배포하는 플랫폼이다.
현재는 구현 전 문서 기준선이며 상태 해석은 [문서 기준선](DOCUMENTATION_BASELINE.md)을 따른다.

## 사용자와 주요 흐름

| 사용자 | 주요 작업 |
| --- | --- |
| 관리자 | 시스템·인증·역할·DB·샤드·백업·업데이트 관리 |
| Feed 운영자 | 소스와 파서 설정·미리보기, 수집·DNS·ASN 작업 실행, 오류 확인 |
| 보안 운영자·검토자 | Profile·예외·Allowlist·모니터링 설정, 변경 비교와 게시 검토 |
| 조회 사용자 | 지표 출처·DNS 변화·정제 및 배포 등록 기간·대시보드 확인 |
| Feed 소비자 | 허용된 접근 모드와 형식으로 불변 출력 다운로드 |

역할 이름은 예시이며 권한은 [인증·권한](IDENTITY_ACCESS.md)의 내부 RBAC로 평가한다.

## 범위

- IPv4/IPv6, CIDR, Domain, URL 및 여러 입력 파서 지원.
- DNS Resolver 비교·변경 이력과 ASN/CIDR/소유자/국가 보강. GIS 기능은 제외.
- 기본 Profile·복제, 여러 Source·Allowlist 그룹 선택과 설명·근거 관리.
- 직접 수집 지표와 DNS 등 파생 지표 분리, 출처 수와 등록 기간 추적.
- IPv4 /24 고유 IP 임계치 기반 CIDR 승격과 안전한 예외 처리.
- 특정 IP·CIDR의 수집 예외, 모니터링 및 알림.
- 공개·IP 제한·계정 인증·계정과 IP 동시 제한 배포.
- 로컬 계정·LDAP/AD·SAML, 역할·권한과 감사.
- Docker 및 소스 설치, 릴리스 기반 업데이트, 로그·백업·복구.
- MariaDB/PostgreSQL 공통 기능, Timescale 확장과 HA, 출력 Feed 중심 애플리케이션 샤딩.

## 핵심 처리 계약

원문을 수집하고 정규화된 관측을 저장한 뒤 모니터링과 수집 예외를 평가한다.
보강·정제 정책을 적용하여 출력 Feed별 membership을 만들고,
파생·집계 후 최종 Allowlist를 적용한다. 고정된 입력·정책·샤드 라우팅 버전으로
완성한 산출물만 공개한다. 요청마다 전체 Feed를 다시 계산하지 않는다.

Source 수집 성공, 지표의 정제 등록, 실제 Feed 게시, 소비자의 다운로드는 서로 다른 사건이다.
각 시각과 이력을 구분하고 수집 실패를 정상 빈 결과나 안전 판정으로 취급하지 않는다.

## 범위 밖과 준비 조건

이번 작업은 문서만 등록한다. 악성 URL 방문·실행, 능동 스캔, GIS, 무단 데이터 재배포는 포함하지 않는다.
XML/STIX·플러그인, DoT/DoH, 고급 재균형은 단계적으로 개발할 범위이며 누락 기능으로 삭제하지 않는다.
외부 자료의 최신 API 계약·이용 조건, 구체 버전 호환성과 처리량·RPO·RTO는 구현 전에 검증한다.

[요구사항](REQUIREMENTS.md)은 수용 계약 초안,
[아키텍처](ARCHITECTURE.md)는 기술 방향,
[개발 계획](DEVELOPMENT_PLAN.md)은 구현 순서를 정의한다.
